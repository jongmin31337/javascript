import * as allTypes from './type'; 

const foo: allTypes.FooFunction = function() {
    return ' 아무 쓸모 없어';
}

const iUser: allTypes.IUser = {
    id: '2',
    name: '빌 게이츠',
    email: 'bill@ms.com',
    receiveInfo: false,
    active: 'Y',
}